# dependencies
/node_modules
/.pnp
.pnp.*

# build output
/.next/
/out/
/build

# NOTE: data/snapshots/ is deliberately NOT ignored — the harvested JSON in
# there is the site's database and must be committed for the build to have
# anything to render.

# testing
/coverage
/e2e/shots
/test-results
/playwright-report

# typescript
*.tsbuildinfo
next-env.d.ts

# env
.env*
!.env.example

# hosts
.vercel
.netlify

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.scratch/
.claude/
